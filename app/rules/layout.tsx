import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Rules | osu!sunrise",
  openGraph: {
    title: "Rules | osu!sunrise",
  },
};

export default Page;
